<UserControl
    x:Class="TampaIoT.TankBot.App.Controls.JoyStick"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TampaIoT.TankBot.App.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:fa="using:FontAwesome.UWP"
    mc:Ignorable="d"
    d:DesignHeight="200"
    d:DesignWidth="200">

    <Canvas Width="200" Height="200">
        <Ellipse Width="200" Height="200" Stroke="#007fff" StrokeThickness="3" />
        <Line X1="0" X2="200" Y1="100" Y2="100" Stroke="#007fff" StrokeThickness="2" />
        <Line X1="100" X2="100" Y1="00" Y2="200" Stroke="#007fff" StrokeThickness="2" />

        <fa:FontAwesome  Icon="Circle" Canvas.Left="{Binding JoystickDisplayX}" Canvas.Top="{Binding JoystickDisplayY}" Margin="-15"  />

    </Canvas>
</UserControl>
